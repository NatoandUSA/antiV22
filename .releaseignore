# Files/dirs excluded from the delivery package (py main.py package release).
# .env is ALSO hardcoded-excluded in src/packaging.py so it can never leak.
#
# NOTE: everything under data/ is handled by a WHITELIST in src/packaging.py
# (DATA_REFERENCE_KEEP) — only the curated supplier reference files ship; real
# DBs (app.db/agent.db) and all business data are excluded and cannot be
# re-enabled by editing this file. The data/ entries below are kept for humans.
.env
.env.*
!.env.example
.git/
# Runtime research/business data at the repo root (regenerated by harvest/daily-run):
keyword_data.csv
social_signals.csv
supplier_products.csv
# Real DBs + all runtime data under data/ (see DATA_REFERENCE_KEEP whitelist):
*.db
data/app.db
data/agent.db
__pycache__/
.pytest_cache/
.ruff_cache/
.venv/
logs/
dist/
reports/
data/raw/
data/processed/
reports/runs/*/archive_debug_reports/
*.pyc
*.log
*.pem
*.har
*.zip
.DS_Store
Thumbs.db
.cloudflared/
deploy/*.json
